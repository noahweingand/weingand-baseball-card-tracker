import { Component } from 'react';

class LogCard extends Component {
    render() {
        return (
            <div>
                yoooo this is gonna be the log card component
            </div>
        )
    }
}

export default LogCard;